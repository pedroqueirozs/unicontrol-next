# Sessão 2026-07-18

## Objetivo da Sessão

Conferência crítica do módulo de Estoque, motivada pela empresa estar simultaneamente fazendo a contagem física do estoque e já registrando saídas reais de pedidos — qualquer inconsistência nesse período afeta dados de produção. Pedro pediu uma revisão de vulnerabilidades e pontos de atenção, com foco em integridade dos dados.

---

## O que foi encontrado

Revisão das rotas `src/app/api/stock/*` e dos componentes `src/app/(app)/stock/*`. Isolamento por `companyId` e autenticação estavam corretos em todas as rotas. Três problemas de integridade foram identificados, todos ligados a condições de corrida (race conditions) — o tipo de bug que só aparece com uso concorrente, exatamente o cenário atual da empresa:

1. **Saída de estoque podia ficar negativa.** A validação "tem estoque suficiente?" era feita fora da transação, lendo um valor que podia já estar desatualizado quando a escrita acontecia. Duas saídas simultâneas do mesmo produto podiam passar na validação e o estoque final ficar negativo, sem nenhum aviso.
2. **Código sequencial do produto podia duplicar.** `nextCode = max(code) + 1` era calculado fora de transação e sem constraint de unicidade no banco. Dois cadastros simultâneos podiam gerar o mesmo código — e como o bipador de Entrada/Saída resolve o produto pelo código escaneado, isso significava risco real de lançar a movimentação no produto errado.
3. **Entrada em lote não era atômica.** O front-end enviava uma requisição HTTP por produto num loop. Se um item do meio falhasse, os itens anteriores já tinham sido gravados, mas a UI não refletia isso — um novo clique em "Confirmar" duplicava a entrada dos itens já aplicados.

---

## Correções aplicadas

### 1. Saída de estoque — decremento condicional atômico
`src/app/api/stock/movements/out/route.ts` — o decremento agora é um `updateMany` com `WHERE currentStock >= quantidade` dentro de uma transação interativa (`prisma.$transaction(async (tx) => ...)`). Se `count === 0` (outra saída consumiu o estoque entre a leitura e a escrita), a transação inteira é abortada e o cliente recebe um erro claro pedindo para recarregar. A checagem "amigável" anterior (mensagem com "Disponível: X unidades") foi mantida como primeira camada, mas a garantia real agora é a atômica.

### 2. Entrada de estoque — lote atômico
`src/app/api/stock/movements/in/route.ts` passou a aceitar `items: []` (igual à rota de saída) em vez de um item por vez. `src/app/(app)/stock/page.tsx` (`handleMovementInBatch`) foi ajustado para enviar o carrinho inteiro numa única requisição. Toda a entrada é gravada em uma única transação — falha de um item aborta o lote inteiro, sem risco de aplicação parcial.

### 3. Código de produto — unicidade garantida pelo banco
- `prisma/schema.prisma`: adicionado `@@unique([companyId, code])` em `StockProduct`.
- Migration `20260718033248_stock_product_code_unique` — cria `CREATE UNIQUE INDEX "StockProduct_companyId_code_key"`.
- `src/app/api/stock/products/route.ts` (POST): em caso de colisão de código (erro `P2002` do Prisma), recalcula o próximo código e tenta novamente (até 5 tentativas) em vez de propagar o erro ao usuário.

---

## Decisões Técnicas

- **Verificação antes de migrar:** antes de aplicar a constraint única em produção, rodei uma consulta somente leitura contra o banco (`82.25.75.171`) para confirmar que não havia códigos duplicados nos 2080 produtos existentes — se houvesse, a migration teria falhado ao tentar criar o índice. Resultado: 0 duplicados, migration aplicada sem necessidade de corrigir dados antes.
- **`prisma migrate status` como checagem de segurança:** confirmado que as 11 migrations já aplicadas em produção batiam exatamente com as pastas locais antes de criar a 12ª — não havia drift de schema.
- **Migration escrita manualmente**, sem `prisma migrate dev` (que exigiria shadow database e poderia gerar side effects contra o banco de produção, já que não existe banco de dev separado — o `.env` aponta direto pra produção). O SQL foi escrito à mão seguindo a convenção de nomes que o Prisma geraria, e aplicado só com `prisma migrate deploy` após confirmação explícita de Pedro.
- **Índice único em vez de `NOT NULL` + unicidade total:** `code` continua opcional (`Int?`); o índice único do Postgres ignora `NULL` automaticamente, então múltiplos produtos sem código continuam permitidos (nenhum produto atual está nessa situação, mas a coluna é opcional por design).

---

## Verificações feitas

- `npx tsc --noEmit` — sem erros.
- `npx eslint` nos arquivos alterados — nenhum erro novo introduzido (os avisos existentes são em código não tocado nesta sessão).
- Pós-migration: índice confirmado via `pg_indexes`, contagem de produtos (2080) inalterada, `prisma migrate status` limpo.

---

## Commits desta sessão

Nenhum commit foi feito na primeira parte — mudanças aplicadas nos arquivos e a migration já foi executada em produção, mas o commit ficou pendente de pedido explícito do Pedro (regra do projeto). Ver commits da parte 2 abaixo.

---

## Pendências / Ideias para o futuro

- Os 3 erros de lint pré-existentes identificados (`react-hooks/set-state-in-effect` em `page.tsx`, `product-modal.tsx` e `label-print-modal.tsx`) não foram tocados por estarem fora do escopo desta revisão — considerar limpar em uma sessão futura dedicada a isso.
- Vale revisar se outros módulos com lançamentos em lote (ex: futura implementação de Financeiro) seguem o mesmo padrão de transação atômica usado agora em Estoque.

---
---

# Parte 2 — Estorno e Ajuste de Estoque

## Objetivo

Depois da revisão de integridade acima, Pedro perguntou como um operador corrige um lançamento errado hoje — resposta: não havia mecanismo nenhum, só lançar uma movimentação manual contrária, sem rastreabilidade. Foram implementadas duas funcionalidades novas para cobrir os dois cenários reais da operação:
1. **Estorno** — corrigir um lançamento pontual errado (bipou errado, quantidade errada).
2. **Ajuste** — corrigir uma divergência encontrada na contagem física, sem saber qual lançamento passado causou a diferença.

## Decisões de negócio (definidas com o Pedro)

- **Janela de estorno livre: 24h.** Qualquer operador pode estornar um lançamento seu ou de outro operador dentro de 24h da criação. Depois disso, só `admin`/`administrativo` consegue.
- **Permissão do Ajuste: livre para todos os papéis**, mesmo nível de acesso que já existe hoje para lançar entrada/saída (RN-18 não restringe Estoque a nenhum papel).

## O que foi implementado

### Schema (`prisma/schema.prisma`)
`StockMovement` ganhou 4 campos opcionais (migration `20260718040059_stock_movement_reversal_and_adjustment`, só `ADD COLUMN`, nenhuma linha existente alterada):
- `reversalOfId` / `reversedAt` — usados pelo estorno.
- `previousStock` / `newStock` — usados pelo ajuste, pra mostrar o antes/depois no histórico sem precisar recalcular.
- `type` passou a aceitar 4 valores: `"entrada" | "saida" | "estorno" | "ajuste"`.

### Estorno — `POST /api/stock/movements/[id]/reverse`
- Só permite estornar `type: "entrada"` ou `"saida"` que ainda não tenham `reversedAt`.
- Checa a janela de 24h com `isAdminLevel(session.user.role)` (`src/lib/roles.ts`) pra quem está fora da janela.
- Dentro da transação: reivindica o estorno com um `updateMany` condicional (`WHERE reversedAt = null`) antes de tocar no estoque — se duas pessoas tentarem estornar o mesmo lançamento ao mesmo tempo, só a primeira consegue (mesma lição de concorrência da Parte 1).
- Se a reversão for tirar estoque (estornando uma entrada), usa o mesmo guard condicional (`WHERE currentStock >= quantidade`) da saída normal — evita ficar negativo se o produto já foi consumido depois da entrada errada.
- Cria uma nova movimentação `type: "estorno"` com `reversalOfId` apontando pro lançamento original e motivo automático (`"Estorno de: <motivo original>"`).

### Ajuste — `POST /api/stock/movements/adjust`
- Recebe `productId`, `countedStock` (quantidade contada fisicamente) e `reason` (obrigatório).
- Calcula o delta e usa `updateMany` condicional (`WHERE currentStock = <valor lido>`) pra garantir que o estoque não mudou entre a leitura e a escrita — se mudou (ex: uma saída no meio do caminho), rejeita com HTTP 409 pedindo pra recarregar, em vez de sobrescrever.
- Grava `previousStock`/`newStock` na movimentação pra o histórico mostrar a contagem sem precisar cruzar dados.

### Interface
- **Histórico** (`history-tab.tsx`): botão "Estornar" por linha (entrada/saída não estornadas), badges para os 4 tipos, indicação "Estornada" no lançamento original e "antes → depois" nos ajustes. Confirmação via diálogo em `page.tsx` antes de chamar a API.
- **Estoque** (`products-tab.tsx`): novo ícone "Ajustar estoque" por produto, abre `adjust-stock-modal.tsx` (mostra estoque atual, campo de quantidade contada, motivo obrigatório, preview do delta).

## Verificações feitas

- `npx tsc --noEmit` — sem erros.
- `npx eslint` — sem erros novos de um tipo diferente dos 3 já pré-existentes (removi um `Date.now()` chamado durante o render em `history-tab.tsx` que a regra de pureza do React acusou — era só um enfeite visual de tooltip, removido em vez de contornado).
- `npx next build` completo — passou limpo, as duas rotas novas aparecem registradas (`/api/stock/movements/[id]/reverse`, `/api/stock/movements/adjust`).
- Migration verificada antes de aplicar: só `ADD COLUMN` opcionais, sem risco a dado existente; `prisma migrate status` confirmou drift zero antes e depois.
- **Pedro testou manualmente em produção e confirmou que funcionou** (estorno e ajuste).

## Commits desta sessão (parte 2)

- `feat: add stock movement reversal and physical count adjustment`
- Migration aplicada: `20260718040059_stock_movement_reversal_and_adjustment`

---
---

# Parte 3 — Bloqueio de exclusão de produto com estoque

## Objetivo

Pedro notou que excluir um produto hoje só tem um modal de confirmação simples, sem diferenciar produto zerado de produto com estoque real. Perguntou se fazia sentido adicionar mais segurança, principalmente pra produtos com quantidade em estoque.

## Decisão

Entre "digitar o nome pra confirmar" e "bloquear direto quando há estoque", Pedro escolheu **bloquear**: um produto com `currentStock > 0` não pode ser excluído — precisa zerar o estoque primeiro (via Ajuste ou Saída). Produto já zerado mantém o fluxo simples de sempre, sem fricção extra (RN-13 já observa que muitos produtos ficam zerados por tempo indeterminado — não fazia sentido adicionar fricção onde não há risco).

## O que foi implementado

- `DELETE /api/stock/products/[id]` (`src/app/api/stock/products/[id]/route.ts`) — retorna HTTP 422 com mensagem clara se `currentStock > 0`. Essa é a garantia real, validada no servidor, não só na tela.
- Modal de exclusão (`src/app/(app)/stock/page.tsx`) — passou a ter dois estados:
  - Produto com estoque: título muda para "Não é possível excluir", mostra a quantidade parada, e troca o botão "Excluir" por **"Ajustar estoque"**, que fecha o modal de exclusão e já abre o modal de ajuste pra aquele produto (usa o mesmo `adjustProduct`/`AdjustStockModal` da Parte 2).
  - Produto zerado: mantém o modal simples de confirmação de hoje.
- `handleDeleteProduct` também passou a repassar a mensagem de erro específica do servidor via toast, em vez de um erro genérico.

Nova regra documentada: `RN-22` em `docs/regras-de-negocio.md`.

## Verificações feitas

- `npx tsc --noEmit` — sem erros.
- `npx eslint` — nenhum erro novo (só o mesmo pré-existente de `loadData()` em `page.tsx`, já documentado na Parte 1).
- `npx next build` completo — compilou sem erros.
- Não precisou de migration — `currentStock` já existia na tabela.
- **Pedro testou manualmente em produção e confirmou que funcionou.**

## Commits desta sessão (parte 3)

- `feat: block deleting stock products that still have quantity on hand`

---
---

# Parte 4 — Paginação do histórico + saldo rastreável em toda movimentação

## Objetivo

Pedro perguntou duas coisas sobre a aba Histórico: (1) já tem paginação? e (2) como o histórico é rastreável, já que não dá pra ver o saldo antes de uma movimentação (só ajuste tinha isso). Confirmamos que não havia paginação nenhuma — a API tinha um limite fixo de `take: 200` sem forma de pedir mais — e que reconstituir o saldo de uma entrada/saída antiga dependia de somar tudo manualmente, sem garantia nenhuma contra o limite dos 200. Pedro pediu pra corrigir os dois, com o cuidado de não estragar nada do que já estava cadastrado.

## O que foi implementado

### Saldo em toda movimentação
`previousStock`/`newStock` (colunas já criadas na Parte 2, usadas só por "ajuste" até aqui) passaram a ser gravadas em **todo** tipo de movimento:
- `movements/in/route.ts` — transação interativa (em vez de array de promises) porque cada `update()` retorna a linha atualizada, usada pra calcular `previousStock`/`newStock` sem consulta extra.
- `movements/out/route.ts` — o guard condicional (`updateMany` com `currentStock >= quantidade`) não retorna a linha, então uma busca extra (`findUniqueOrThrow`) dentro da mesma transação pega o saldo resultante.
- `movements/[id]/reverse/route.ts` — os dois ramos (estornar entrada / estornar saída) passaram a calcular e gravar o saldo também.
- `movements/adjust/route.ts` já gravava isso desde a Parte 2 — nenhuma mudança necessária ali.

### Paginação de verdade
- `GET /api/stock/movements` — passou a aceitar `page`, `pageSize` (padrão 50, máx. 100) e `type`, filtrando e paginando no banco (`skip`/`take` + `count`), sem o limite fixo de 200 de antes.
- `history-tab.tsx` foi reescrito pra ser dono dos próprios dados: busca sob demanda conforme página/filtro mudam (antes recebia a lista pronta de `page.tsx`), com os mesmos controles de paginação já usados na aba Estoque. O diálogo de confirmação de estorno e o `handleReverseMovement` também migraram pra dentro do componente (fazia mais sentido morar junto de quem busca os dados).
- `page.tsx` ficou mais simples: `loadData` agora só busca produtos; `HistoryTab` recebe um `onReversed` (= `loadData`) pra avisar quando um estorno mudar o estoque de um produto, mantendo os dados de `products` sincronizados mesmo estando em outra aba.
- Coluna "Saldo (antes → depois)" adicionada na tabela/cards do histórico, mostrando `previousStock → newStock` pra qualquer tipo de movimentação, não só ajuste.

### Backfill do histórico já existente
Sem migration nova (colunas já existiam). Para preencher as 62 movimentações antigas (entrada/saída sem saldo gravado), criado `scripts/backfill-movement-balances.ts`:
- Reconstrói o saldo de cada produto reproduzindo suas movimentações em ordem cronológica a partir de 0 (invariante confirmado por grep: só existem dois lugares que criam `StockProduct`, ambos com `currentStock: 0` — cadastro manual e `scripts/import-products.ts`).
- Antes de gravar, confere se o saldo final reconstruído bate com o `currentStock` atual do produto; se não bater, pula esse produto e avisa em vez de gravar algo arriscado.
- Rodado primeiro em modo simulação (`npx tsx scripts/backfill-movement-balances.ts`, sem `--apply`) — 0 divergências nas 62 movimentações.
- **Incidente durante a aplicação:** a primeira tentativa (`--apply`) envolvia todos os 62 updates numa única transação Prisma, que estourou o timeout padrão de 5s por causa da latência até a VPS (`82.25.75.171`) — Prisma tentou reverter e o rollback também não completou a tempo, retornando erro `P2028`. Antes de qualquer outra ação, rodei a simulação de novo pra confirmar que nada tinha sido escrito parcialmente (confirmado: 0 já corretas, 62 pendentes — idêntico a antes da tentativa). Troquei a estratégia: como cada update é determinístico e idempotente (sempre recalcula o mesmo valor a partir do histórico), removi a transação única e passei a fazer updates sequenciais independentes — mais lento, mas sem risco de ficar pela metade, e seguro pra rodar de novo se cair no meio. Segunda tentativa: sucesso, 62/62 atualizadas.
- Verificado depois: contagem de movimentações inalterada (62), nenhuma linha sem saldo, e rodar o script de novo em modo simulação mostra "62 já corretas / 0 a atualizar" — confirma que o backfill é exato e idempotente.

## Verificações feitas

- `npx tsc --noEmit` — sem erros.
- `npx eslint` — nenhum erro novo (só o mesmo `set-state-in-effect` pré-existente, agora também presente no `load()` de `history-tab.tsx`, seguindo o mesmo padrão já usado no resto do arquivo).
- `npx next build` completo — compilou sem erros, rotas registradas corretamente.
- Backfill: simulação → aplicação → simulação de novo, com o banco consultado diretamente em cada etapa pra confirmar contagens e saldos.
- **Pedro testou manualmente em produção e confirmou que tudo está funcionando perfeitamente.**

## Commits desta sessão (parte 4)

- `feat: add pagination and per-movement running balance to stock history`
